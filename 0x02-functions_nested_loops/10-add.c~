#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 *
 * @a: interger 1
 *
 * @b: integer 2
 *
 * Return: the results
 */

int add(int a, int b)
{
results = add(a, b);
_putchar(results + '0');
return (results);
}

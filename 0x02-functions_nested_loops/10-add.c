#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 *
 * @a: interger 1
 *
 * @b: integer 2
 *
 * Return: the results
 */

int add(int a, int b)
{
int results = add(a, b);

if (results >= 0)
{
_putchar(results + '0');
}
else
{
_putchar(results *= -1 + '0');
}
return (results);
}

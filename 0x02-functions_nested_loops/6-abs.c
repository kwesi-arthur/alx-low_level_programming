#include "main.h"

/**
 * _abs - provides the absolute value of an integer.
 *
 * @i: the character whose abolute will be checked
 *
 *Return: Always 0 (success)
 */

int _abs(int i)
{
if (i >= 0)
return (i);
else
return (-i);
}
